apiVersion: v1
kind: Service
metadata:
  name: gateway-service
spec:
  type: NodePort
  selector:
    app: gateway
  ports:
    - port: 8080        # internal cluster port
      targetPort: 8080    # container port (where the app listens)
      nodePort: 30081     # host port (what you hit from outside)
